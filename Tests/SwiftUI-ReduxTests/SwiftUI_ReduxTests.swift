import XCTest
@testable import SwiftUI_Redux

final class SwiftUI_ReduxTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwiftUI_Redux().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
